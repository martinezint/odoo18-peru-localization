<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_partner_inbox_message_tree" model="ir.ui.view">
    <field name="name">l10n.pe.partner.inbox.message.tree</field>
    <field name="model">l10n.pe.partner.inbox.message</field>
    <field name="arch" type="xml">
      <list string="Bandeja XML proveedores" decoration-danger="state == 'error'" decoration-success="state == 'processed'">
        <field name="create_date"/>
        <field name="email_from"/>
        <field name="subject"/>
        <field name="state"/>
        <field name="created_move_ids" widget="many2many_tags"/>
      </list>
    </field>
  </record>

  <record id="view_partner_inbox_message_form" model="ir.ui.view">
    <field name="name">l10n.pe.partner.inbox.message.form</field>
    <field name="model">l10n.pe.partner.inbox.message</field>
    <field name="arch" type="xml">
      <form>
        <header>
          <field name="state" widget="statusbar"/>
        </header>
        <sheet>
          <group>
            <group>
              <field name="email_from"/>
              <field name="subject"/>
              <field name="company_id" groups="base.group_multi_company"/>
            </group>
            <group>
              <field name="create_date"/>
              <field name="created_move_ids" widget="many2many_tags" readonly="1"/>
            </group>
          </group>
          <notebook>
            <page string="Log de procesamiento">
              <field name="processing_log" widget="text" readonly="1"/>
            </page>
          </notebook>
        </sheet>
        <chatter/>
      </form>
    </field>
  </record>

  <record id="action_partner_inbox_message" model="ir.actions.act_window">
    <field name="name">Bandeja XML proveedores</field>
    <field name="res_model">l10n.pe.partner.inbox.message</field>
    <field name="view_mode">list,form</field>
  </record>

  <menuitem
      id="menu_partner_inbox_message"
      name="Bandeja XML proveedores"
      parent="account.menu_finance_entries"
      action="action_partner_inbox_message"
      sequence="50"/>
</odoo>
